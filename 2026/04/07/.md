
---
layout: post  
title: "学习Linx"
date: 2026-04-07
tags: [GitHub, 博客]
---

视频时长：104min     完成预计时长：150min  
完成标识：centos在虚拟机跑起来  
遇到的问题：下载虚拟机的网站中的下载链接不能用
centos的系统下载  
解决方案：下载了压缩版的并用迅雷下载和解压缩      


M2熟悉linx的基础操作指令  
视频时长：104min   完成预计：150min  
完成标识：能使用各项Linx基础操作指令，能看懂并理解目录及路径  
实际完成时间：180min  
总结及学习内容：  
相对路径和绝对路径的区分，相对路径更加简便是起始路径/末尾路径，绝对路径是沿着路径到最后。  
ls -a -l -h 路径 。展示文件夹的目录。-a（all）能展示出一些隐藏的文件文件夹。-l以列的形式展示目录同时会有其他的信息左到右权限、所属用户、所属用户组等。
cd 路径.更改目前所属的路径。  
pwd 路径。展示所属路径。  
mkdir  路径文件夹名。创建新的文件夹。-p可以将所属路径上的未有的文件夹全部创建。  
touch  路径文件名。创建新文件。  
cat  路径文件名。查看文件内容。  
more  查看文件内容内容过多时可以翻页。通过空格进行翻页q退出。  
cp -r 被复制文件 复制到的路径。复制文件文件夹。-r文件夹及其文件内容。  
mv -r 文件文件夹名 要去的地方。  
rm -r -f  文件文件夹名 。可以多个。  
which 查看命令所在文件。  
find 起始路径 -name "被查文件名"。  
find 起始路径 -size+（&-）数字kMG。  
grep -n 关键字 路径。在路径中查找关键字的内容，-n显示行数。重定向符>左侧内容覆盖到右侧文件中>>左侧内容追加到右侧文件中。  
wc -c -m -l -w 路径。-c显示byte数量-m显示字符数量-l行数-w显示单词数。  
echo 输出内容。在命令行显示内容。复杂内容用"".反引号``中内容当作命令。  
tail -f -num（确切数字）路径。查看文件尾部内容。-f持续追踪。-num查看行数不加默认10行。  
vi/(vim) 文本编译器。  
通配符*test以test结尾的*test*含有的test*以他开头的。  
管道符|左侧输出作为右侧输入。  
### **`chmod`**：修改文件或目录的权限

**注意：**

> 如果想要给文档设置权限，操作者要么是**root用户**，要么就是**文档的所有者**。

```
chmod 选项 权限信息 文档
-R：递归设置权限 （当文档类型为文件夹的时候）
权限信息：就是对该文档的不同身份使用者需要设置的权限信息
文档：可以是文件，也可以是文件夹，可以是相对路径也可以是绝对路径。

权限信息参数：
给谁设置：
u：表示所有者身份owner（user）
g：表示给所有者同组用户设置（group）
o：表示others，给其他用户设置权限
a：表示all，给所有人（包含ugo部分）设置权限
如果在设置权限的时候不指定给谁设置，则默认给所有用户设置

权限字符：
r：读
w：写
x：表示执行
-：表示没有权限

权限分配方式：
+：表示给具体的用户新增权限（相对当前）
-：表示删除用户的权限（相对当前）
=：表示将权限设置成具体的值（注重结果）【赋值】
给anaconda-ks.cfg文件（-rw-------.）设置权限，要求所有者拥有全部的权限，同组用户拥有读和执行权限，其他用户只读权限。
#chmod u+x,g+rx,o+r anaconda-ks.cfg
#chmod u=rwx,g=rx,o=r anaconda-ks.cfg
```

```
补充：

可以使用root用户设置所有人都有执行权限，可以写成
①#chmod +x anaconda-ks.cfg
②#chmod a=x anaconda-ks.cfg
③#chmod a+x anaconda-ks.cfg
a意为all，可忽略
```

### **`chown`**：改变文件或目录的所有者

```
chown -R username:groupname 文档路径

将lib目录的所属用户和用户组改成codegoat24，并且包含其子目录
#chown -R codegoat24:codegoat24 ./lib
```

指令	作用	常用参数与示例  
sudo	以管理员权限执行	sudo apt update (Debian 系更新软件源)。  
whoami	查看当前用户	确认自己是否是 root 用户。  
df/du	磁盘空间	df -h (查看磁盘剩余空间)、du -sh . (查看当前目录占用大小)。  
top	监控进程 / 资源	查看 CPU、内存占用，按 q 退出。  
ping	测试网络连通性	ping www.baidu.com (检查网络是否通)。  
ifconfig/ip a	查看网络 IP	ip a (查看本机 IP 地址，网络工程常用)。  
chmod	修改权限	chmod 755 post.sh (赋予脚本可执行权限)。  
指令	作用	常用参数与示例  
tar	打包 / 解压	tar -zcvf test.tar.gz dir/ (打包并压缩)、tar -zxvf test.tar.gz (解压)。  
curl	测试 URL / 下载	curl -O https://example.com/file.zip (下载文件)。  
wget	下载网络文件	wget https://github.com/.../repo.zip (下载压缩包)。  
